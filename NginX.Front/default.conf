# the names 'backend' and 'frontend' are hostnames defined in the docker-compose.yml file as services. If the servicename changes, change here too.
upstream backend {
    server backend:80;
}

server {
    listen 80;

    location / {
        proxy_pass http://backend;
    }
}